import * as React from 'react'
import ReactDOM from "react-dom";
import {Box, Container} from "@mui/material";

function Model() {
    return (
        <Container component={"div"}>
            <Box sx={{bgcolor: "red",}} maxWidth={true}>
                <div>SAlutttt</div>
            </Box>
        </Container>
    );
}

export default Model;
